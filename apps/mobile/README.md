# Rehla customer application

The customer-facing Flutter application is intentionally reserved for its
planned implementation phase. Its API boundary is defined now in
`backend/openapi/openapi.yaml`; customer business rules remain in Go.
