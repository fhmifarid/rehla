# Project memory

This directory is the persistent engineering memory for Rehla. Read every file
before starting a new implementation session, begin from `current-task.md`, and
update the affected maps and decisions after each meaningful milestone.

`Plan.md` remains the functional product specification. Project memory records
what has actually been decided, implemented, verified, deferred, or found to be
problematic.
